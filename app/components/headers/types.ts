import { PageColorMode, PageType } from '~/types/page';
export type SharedHeaderProps = {
  absolute : boolean,
  color : string,
  mode: PageColorMode,
  isDark : boolean,
};

export default SharedHeaderProps;