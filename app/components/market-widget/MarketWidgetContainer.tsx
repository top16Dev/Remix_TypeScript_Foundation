import { styled } from '~/stitches.config';
import Box from '~/components/base/Box';

const MarketWidgetContainer = styled(Box, {
  borderRadius: 24,
  border: 'solid 1px $black10',
});

export default MarketWidgetContainer;
